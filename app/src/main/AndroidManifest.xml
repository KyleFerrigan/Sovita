<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.kernelpanic.sovita">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sovita"
        android:usesCleartextTraffic="true"
        tools:targetApi="m"
        android:fullBackupContent="@xml/backup_descriptor"
        tools:ignore="AllowBackup">
        <activity
            android:name=".WorkoutScreen"
            android:label="@string/title_activity_workout_screen"
            android:theme="@style/Theme.Sovita.NoActionBar" />
        <activity
            android:name=".WorkoutSaved"
            android:label="@string/title_activity_workout_saved"
            android:theme="@style/Theme.Sovita.NoActionBar" />
        <activity android:name=".WorkoutEdit" />
        <!-- <activity android:name=".NewWorkout_2" /> -->
        <activity android:name=".NewWorkout" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET"
        tools:ignore="ManifestOrder" />

</manifest>